log:
  level: info

database:
  dsn: "postgres://postgres@/anthrimon?host=/var/run/postgresql&sslmode=disable"

polling:
  # How often the engine evaluates all alert conditions (seconds)
  interval_seconds: 15

notifications:
  smtp:
    enabled: false
    host: localhost
    port: 25
    from_addr: "anthrimon@localhost"
    # to_addr: "noc@yourcompany.com"
    # username: ""
    # password: ""

  slack:
    enabled: false
    # webhook_url: "https://hooks.slack.com/services/..."
